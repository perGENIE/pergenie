machine:
    python:
        version: 2.7.6

dependencies:
    pre:
        - pip install -r requirements/ci.txt

test:
    override:
        - cd pergenie && export DJANGO_SETTINGS_MODULE=pergenie.settings.ci && py.test apps/landing/test_browser.py
